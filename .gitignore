.venv/
__pycache__/
*.pyc
.env
.DS_Store

# data/weathercontest.sqlite3 is intentionally tracked - GitHub Actions commits
# daily updates to it. If it grows unwieldy, swap for a small hosted DB instead.
